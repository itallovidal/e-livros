import { styled } from './theme.ts'

export const Main = styled('main', {
  alignItems: 'center',
  margin: 'auto',
  gap: '1rem',
  width: '100%',
  backgroundColor: 'white',
  textAlign: 'center',

  h1: {
    fontSize: '6rem',
  },
})
