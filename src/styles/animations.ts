import { keyframes } from '@stitches/react'

export const showing = keyframes({
  '100%': { transform: 'translateY(0px)', opacity: 1 },
})

export const imageShowing = keyframes({
  '100%': { transform: 'scale(1)', opacity: 1 },
})
